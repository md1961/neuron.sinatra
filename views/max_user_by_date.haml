%table.table_base
  - @date_and_max_users.each do |date, max_users|
    - day_of_week = date[-4 .. -2]
    %tr{:class=>%w(Sat Sun).include?(day_of_week) ? 'weekend' : ''}
      %td
        %div(style='width: 9em')= date
      %td(align='right' style='width: 5em')== #{max_users} users

