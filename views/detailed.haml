%table.table_base
  - @loggedins.each do |entry|
    %tr
      %td
        %div(align='center' style='width: 9em')== #{entry.date} #{entry.time}
      %td(align='right' style='width: 5em')== #{entry.users.size} users
      %td= entry.users.map(&:description).join(', ')

