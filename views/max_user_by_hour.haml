%table.table_base
  - @timestamp_and_max_users.each do |timestamp, max_users|
    %tr{:class=>timestamp.daytime? ? 'daytime' : 'nighttime'}
      %td
        %div(align='center' style='width: 11em')= timestamp
      %td(align='right' style='width: 5em')== #{max_users} users

